"""
Calculate Pr(Document)

In the niave bayes classifier, the calculation of Pr(Document) was skipped
since it wasn't required to compare the probabilities. In cases where the
features are independent, it can also be used to calculate the overall
probability. How would you calculate Pr(Document).
"""
# 1 / set of possible features